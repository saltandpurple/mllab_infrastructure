mlflow:
  postgresql:
    auth:
      existingSecret: "mlflow-postgresql"
  tracking:
    auth:
      existingSecret: "mlflow-tracking"
    persistence:
      storageClass: gp3
    metrics:
      enabled: true
    ingress:
      enabled: false
    hostname: "mlflow.mllab.davidsfreun.de"
